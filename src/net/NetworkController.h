#pragma once

#include <trutle/Trutle.h>

/* NetworkController
 * The Network Controller is responsible for sending and retrieving packets,
 * parsing them and manging the connections. It is not in charge of WHAT the
 * packets contain.
 */
class NetworkController {
public:
	NetworkController();
	~NetworkController();

private:

};
